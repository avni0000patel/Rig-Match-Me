<div class="d-flex justify-content-center vh-100">

  <div
    id="chalkboard"
    class="col-2 p-3 bg-dark d-flex justify-content-start flex-column mb-4"
  >
    <div id="create-rig-list" class="d-flex flex-column">
      <p class="fs-1 text-light">
        Create New Rig
      </p>
      <ul class="text-light fs-2">
        <li class="rig-task invisible">Choose a Genre</li>
        <li class="rig-task invisible">Choose an Intrument Category</li>
        <li class="rig-task invisible">Choose an Instrument Model</li>
        <li class="rig-task invisible">Choose your First Accessory</li>
        <li class="rig-task invisible">Choose your Second Accessory</li>
      </ul>
    </div>

  </div>
  <div
    id="create-rig-staging-area"
    class="col-sm-10 bg-light p-3 mh-100 overflow-auto"
  >
  </div>
  {{!-- <form class="form new-rig-form">
    <div class="form-group">
      <label for="genre">Choose a genre:</label>
      <select class="genre" id="genre" name="genre">
        {{#each genres as |genre|}}
        <option value="{{genre.genre}}">{{genre.genre}}</option>
        {{/each}}
      </select>
    </div>
    <div class="form-group">
      <label for="instrument">Choose an instrument:</label>
      <select class="instrument" id="instrument" name="instrument">
        {{#each genres as |genre|}}
        <option value="{{genre.instrument_type}}">{{genre.instrument_type}}</option>
        {{/each}}
      </select>

      <div class="invisible" id="instrument">
        <label for="instrument">Choose an instrument type:</label>
        <select name=" instrument">
          <option value="Electric Guitar">Electric Guitar</option>
          <option value="Bass Guitar">Bass Guitar</option>
          <option value="Drums">Drums</option>
        </select>
      </div>

    </div>
    <div class="form-group">
      <button type="submit" class="new-rig-form btn btn-primary">Next</button>
    </div>
  </form> --}}
</div>

<script src="/js/new_rig.js"></script>